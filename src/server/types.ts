export enum RoomType {
  PRIVATE = "private",
  DEBATE = "debate",
  RANDOM = "random"
}

export enum DebateRole {
  PRO = "pro",
  CON = "con",
  MODERATOR = "moderator",
  SPECTATOR = "spectator"
}

export enum MessageType {
  TEXT = "text",
  SYSTEM = "system",
  ACTION = "action"
} 